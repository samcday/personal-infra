apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml

secretGenerator:
  - name: autoscaler-cloud-init
    files:
    - data=cloud-init.txt

generatorOptions:
  disableNameSuffixHash: true
